torch>=2.1
numpy
PyYAML
tqdm
trimesh
manifold3d>=3.0
rtree>=1.2
scipy>=1.10
huggingface_hub
matplotlib
zstandard>=0.22
# Optional (visualization only): install open3d for the richer viewer in
# scripts/visualize_inference.py; it falls back to matplotlib if open3d is absent.
# open3d
